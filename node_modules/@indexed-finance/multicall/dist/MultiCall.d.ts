import { JsonFragment } from "@ethersproject/abi";
import { Interface } from "ethers/lib/utils";
import { CallInput, TokenBalances, TokenBalancesAndAllowances, UniswapReservesData } from './types';
export declare class MultiCall {
    private provider;
    constructor(provider: any);
    multiCall(_interface: Interface | JsonFragment[], inputs: CallInput[], strict?: boolean): Promise<[number, any[]]>;
    multiCall(inputs: CallInput[], strict?: boolean): Promise<[number, any[]]>;
    getBalances(tokens: string[], account: string): Promise<[number, TokenBalances]>;
    getBalancesAndAllowances(tokens: string[], owner: string, spender: string): Promise<[number, TokenBalancesAndAllowances]>;
    getReserves(pairs: string[]): Promise<[number, UniswapReservesData]>;
}
